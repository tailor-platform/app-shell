---
"@tailor-platform/app-shell": minor
---

Add `xs` size variant to `Button` component — a compact extra-small button (`h-7`, `text-xs`) for inline actions within cards and tight layouts.

```tsx
<Button size="xs" variant="outline">
  Resubmit
</Button>
```
