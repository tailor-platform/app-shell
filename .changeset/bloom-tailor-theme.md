---
"@tailor-platform/app-shell": minor
---

Add the **Bloom** light palette (`bloom`): lavender shell with cream secondary surfaces — set with `defaultTheme`, `ThemeProvider`, or **`setTheme("bloom")`**. Sidebar theme cycling includes Bloom when using the layout toggle.
