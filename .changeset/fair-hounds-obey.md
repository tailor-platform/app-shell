---
"@tailor-platform/app-shell": patch
"@tailor-platform/eslint-plugin-app-shell": minor
---

Stop documenting AppShell's internal `astw:` CSS prefix as a consumer customization API.

Add an Oxlint preset that prevents consumers from bypassing AppShell's React Router integration or using internal `astw:` classes.
