---
"@tailor-platform/app-shell": patch
---

LineItems: focusing a cell now selects all of its text by default (spreadsheet ergonomics), so a click or tab into a cell puts it in "replace" mode — the next keystroke overwrites the value instead of appending to it. Applies to text, number, and select (combobox) cells.
