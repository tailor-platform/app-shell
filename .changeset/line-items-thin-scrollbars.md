---
"@tailor-platform/app-shell": patch
---

LineItems: the table's scroll container now ships with slim, semi-transparent scrollbars (4px on webkit, `thin` on Firefox) — transparent track, `rgba(0,0,0,0.4)` thumb. Reduces visual noise inside dense tables and inside cards.
