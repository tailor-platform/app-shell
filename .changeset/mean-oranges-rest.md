---
"@tailor-platform/app-shell": major
---

Upgrade AppShell to React Router v8 and raise the minimum supported `react` / `react-dom` version to `19.2.7`.

This keeps AppShell aligned with the React Router version it owns internally while preserving the existing AppShell routing API for consumers.
