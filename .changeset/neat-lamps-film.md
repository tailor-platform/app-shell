---
"@tailor-platform/app-shell": minor
---

Re-export `AppShellRoutesPluginOptions` and `TypedRoutesOptions` from `@tailor-platform/app-shell/vite-plugin` so the subpath can be used as the canonical TypeScript entrypoint.

Update the file-based routing docs and examples to consistently import the Vite plugin from the `@tailor-platform/app-shell/vite-plugin` subpath.
