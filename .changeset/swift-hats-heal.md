---
"@tailor-platform/app-shell-scaffold": minor
---

Add a page-level scaffold CLI for AppShell list screens.

The initial `resource/list` scaffold generates a compile-ready `page.tsx` and page-local table component, with embedded `TODO(app-shell-scaffold)` guidance for wiring generated GraphQL types and generated `tableMetadata` into `useQuery`, `useURLCollectionVariables`, and `useDataTable`.
