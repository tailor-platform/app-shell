---
"@tailor-platform/app-shell": patch
---

Clarify AppShell theme usage around `@tailor-platform/app-shell/styles` and optional `@tailor-platform/app-shell/themes/*` palette imports.

Palette authoring docs and templates now match the current light/dark model, and shared theme aliases stay inherited from the default palette instead of being repeated in each branded theme.
