---
"@tailor-platform/app-shell-vite-plugin": patch
---

Update README to document the `entrypoint` option, `[...slug]` catch-all path conversion, and the current `AppShell.WithPages` implementation.
