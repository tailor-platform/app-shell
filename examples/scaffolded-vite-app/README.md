# Scaffolded Vite App

Minimal Vite app used to verify page-level scaffolds from `packages/scaffold`.

The `procurement/purchase-order` list page was generated from the CLI and checked into this example as a golden runtime sample.

## Run

```bash
pnpm --filter scaffolded-vite-app dev
```
