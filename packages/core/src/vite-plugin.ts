/**
 * Re-export the Vite plugin from @tailor-platform/app-shell-vite-plugin.
 *
 * This allows users to import the plugin from the main app-shell package:
 *
 * @example
 * ```ts
 * import { appShellRoutes } from '@tailor-platform/app-shell/vite-plugin';
 * ```
 */
export { appShellRoutes } from "@tailor-platform/app-shell-vite-plugin";
